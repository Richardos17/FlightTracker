name: Fetch flight prices

on:
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger price fetch
        run: |
          curl -sf -X POST ${{ secrets.APP_URL }}/api/cron/fetch-prices \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
